enum Cz { usd, ngn, unn }

enum ManualUpdateOption { include, replace }

enum MXInputTypes { dropdown, text }

enum UpdateOption { manual, csv }

/// Track Entry Type --Update
enum TETU { expCredit, expDebit, credit, debit }


///TrackEntryType
enum TET { balance, frozen, expCredit, expDebit, credit, debit }